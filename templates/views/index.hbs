<!DOCTYPE html>
<html lang="en">
<head>
    <title>Home</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/img/weather.png">
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@500&display=swap');
        .divStyle{
            height: 15px;
            display: flex;
            justify-content: center;
            align-items: center; 
            margin-top: 40px;
        }

        .divStyleTwo{
            padding-left: 210px;
            margin-top: 50px;
            font-family: 'Poppins', sans-serif;
            font-size: 40px;
            height: 60px;
            display: flex;
            justify-content: center;
            align-items: center; 
        }

        .divStyleThree{
            margin-top: 40px;
            font-family: 'Poppins', sans-serif;
            font-size: 26px;
            height: 40px;
            display: flex;
            justify-content: center;
            align-items: center; 
        }

        input{
            width: 250px;
            height: 30px;
            border: 1px solid #cccccc;
            padding-left: 5px;
            font-size: 16px;
        }

        button{
            height: 32px;
            font-size: 16px;
            cursor: pointer;
            background-color: #888888;
            color: white;
        }

    </style>
</head>
<body>
    <!-- Loading up the partial defined in 'partials', hbs will find it for us 
    given we configured the directory ('hbs.registerPartials(partialsPath)') -->
    {{>header}}
    <div class="divStyleTwo">Get the weather forecast in real time!</div>
    <div class="divStyle">
        <form id="weather">
            <input type="text" placeholder="Enter the location!" id="searchPlace">
            <button>Search</button>
        </form>
    </div>
    <div id="display" class="divStyleThree"></div>
    {{>footer}}
 
    <script type="text/javascript" src="/js/app.js"></script>
</body>
</html>